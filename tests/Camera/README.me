basic_operation.py : Uses RGB camera, Applies the default Neural Network for object detection, and reads IMU data. The imu data is printed in command line and the images are displayed with the detection boxes.

test.py: First try to syncronized saving of images and IMU data. For detailed info on this go to docs folder. 

camera_imageread: Simple code to capture and apply a Neural Network to detect objects.

